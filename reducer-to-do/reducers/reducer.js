export const initialState = {
    item: 'Default To Do',
    completed: false,
    id: 3892987589
};

export function reducer(state, action) {
    switch(action.type) {
        default:
            return state;
    }
}